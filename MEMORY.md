# 猫咪数独：底层规则记忆

本项目后续所有关卡生成、求解验证、难度评估、交互实现和测试，均以本文件为准。

## 棋盘硬规则

1. 每个颜色区域恰好有一只猫。
2. 每一行恰好有一只猫。
3. 每一列恰好有一只猫。
4. 两只猫不能相邻。相邻包括上下、左右及四个对角，共八个方向。
5. 每关必须存在且仅存在一个满足上述规则的解；不得把随机猜测或多解作为难度来源。
6. 每种颜色构成一个且仅一个连续区域；同色格必须通过上下左右相邻连通，不能分裂为多个孤立色块。
7. 关卡生成时，单格颜色区域最多允许存在 1 个；若有 2 个及以上单格区域，必须重新生成。校验和人工编辑也遵循此限制。
8. 起手线索按难度分档：轻松、标准且未开启“开局显示一只猫”时，每关至少有一种颜色区域恰好 1 格；进阶且未开启预显示猫时，至少有一种颜色区域为 1–3 格；开启预显示猫后取消这两档限制；挑战难度始终不受此限制。
9. 轻松、标准、进阶的非预显示关卡必须具备连续推理链：玩家从最小色区找到猫并手动排除同行、同列、同色区和八方向邻格后，应能在剩余颜色区域中找到下一只猫的有限候选线索。轻松/标准候选上限为 2 格，进阶为 3 格；不得把连续试错作为主线解题方式。
10. 所有正式交付关卡必须在生成阶段完成规则验证；若筛选失败，不得交付“待验证”或未验证候选图。无论棋盘尺寸，唯一解都必须由求解器完成验证；连续推理链只用于难度与可玩性评估，不能替代唯一解验证。大尺寸生成需使用高性能、可中断的求解器或离线题库，不能以放弃唯一解为代价。
11. 棋盘尺寸范围为 4×4 至 15×15。8×8 及以上允许适度放宽颜色区域面积均衡差值和下一步候选数量，但唯一解、颜色区域连通、每行/每列恰好一猫、猫八方向不相邻必须始终保留。
12. 生成器采用“植入合法猫位 → 生长连通颜色区 → 精确求解 → 针对第二答案修复边界 → 再次验证”的流程；不得通过放弃唯一解来换取大尺寸生成成功。大尺寸允许更宽的面积差与候选数，但最终仍须由精确求解器确认唯一答案。
13. 10×10 及以上的非挑战关卡优先使用构造式推理链：第一个颜色区为单格线索；第 k 个颜色区的非猫格必须已被前 k−1 只猫的行、列或八方向规则排除，从而形成确定的连续推理路径。构造后仍必须运行精确求解器验证唯一解；若构造批次失败应快速重新生成，不能退回到长时间的随机修复搜索。

## 玩家目标与生命

1. 胜利条件是玩家通过双击找出棋盘中全部猫。
2. 不要求标记全部空格，也不要求清除全部普通 X。
3. 玩家初始有生命值；双击空格会扣除一格生命。
4. 生命值耗尽后，玩家可以通过观看广告恢复生命。具体广告次数和恢复量可以另行设计。
5. 关卡棋盘边长必须可配置，不能在生成器或客户端逻辑中写死为特定尺寸。

## 初始预放猫配置

1. 关卡支持 `initialRevealedCats` 配置，表示开局已显示的真实猫位置。
2. 预放猫必须来自该关的唯一答案，且视为已经找到的猫：占用其颜色区域、所在行、所在列，并参与相邻限制。
3. 预放猫不是玩家双击所得：不计入双击得分，不消耗生命，也不触发玩家操作奖励。
4. 预放猫格与玩家找出的猫格一致，均不可再操作；其余格不会因预放猫而自动添加普通 X。
5. 预放猫仅作为教学、前期难度控制或特殊玩法参数。常规关卡默认不预放；配置数量必须小于该关猫总数，避免开局自动胜利。

## 编辑器 JSON 格式

1. 编辑器导出的字段统一使用首字母大写：`Id`、`Size`、`Regions`、`Animals`、`Revealed`、`Difficulty`。
2. `Animals` 表示唯一答案中全部猫的位置数组；不再导出 `cats` 字段。
3. `Id` 为大于 0 的整数，默认 `1`，用于区分不同关卡配置。
4. 编辑器导入时兼容历史小写字段（包括 `cats`），但再次导出时必须转换为上述标准字段格式。

## 格子交互规则

### 单击：普通 X（玩家笔记）

1. 对未锁定、未找出猫的格子单击，会添加普通 X；再次单击会取消普通 X。
2. 普通 X 仅表示玩家主观排除，不校验答案、不扣生命、不影响胜负。
3. 真猫格也可以被标为普通 X；其后仍可通过双击正确找出猫。

### 双击：正式找猫

1. 双击真猫格：显示猫、计入已找出进度；该格随后不可再操作。
2. 双击空格：显示红色 X、扣除一格生命；该格永久锁定，不可再操作。
3. 已找出的猫格和已显示红色 X 的错误格，再次单击或双击均无效。
4. 找出一只猫后，游戏不自动给同行、同列、同色区域或相邻格添加排除标记；所有普通 X 均须由玩家手动操作。

## 设计边界

1. 普通 X 是自由笔记；错误成本只来自双击空格。
2. 关卡须能纯逻辑推导完成。生命与广告恢复用于容错和商业化，不可用于掩盖多解或无解关卡。
3. 所有界面和规则文案中，应将单击称为“标记/排除”，将双击称为“寻找/确认猫的位置”，避免把普通 X 表述为系统判定。
